import { ResponseGoogleLogin } from './response-google-login.model';

describe('ResponseGoogleLogin', () => {
  it('should create an instance', () => {
    expect(new ResponseGoogleLogin()).toBeTruthy();
  });
});
